# @desc "The starting point of the program."
# @ret "Always void."
func main() : void {
	// Loads the string "Hello, World!" into memory.
	ld.str "Hello, World!"

	/*
		Calls a runtime defined function.
		$ represents calling a runtime-defined function.
	*/
	call void $System.Console.WriteLine(str)
}